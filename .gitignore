# macOS
.DS_Store
._*

# Node / packager caches (in case a build step ever gets added)
node_modules/
dist/
build/

# Local Claude Code dev tooling — launch.json has absolute paths + serve.js
# is host-specific. Keep local only.
.claude/

# Editor local state
.vscode/
.idea/
*.swp
