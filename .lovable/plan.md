
# Empty Project Shell

Set up a minimal placeholder so you can connect and sync your GitHub repo.

## What I'll do
- Leave the default project structure as-is
- Replace the placeholder `Index` page with a simple "Ready to sync" message so it's clear the project is intentionally empty and waiting for your repo
- No dependencies, routes, or features added

## Next step (on your end)
- Connect GitHub via **Connectors → GitHub** and sync your repo
- Once synced, we'll continue from your codebase
